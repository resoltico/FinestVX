# direnv configuration for FinestVX
# Ensures all uv commands use the versioned venv, preventing the default
# .venv from being created alongside the scripts' .venv-3.14.
export UV_PROJECT_ENVIRONMENT=".venv-3.14"
